// = assignment aperators
// == comparision operators
// === strict equality operators (compare if values & datatype are equal)
// != inequality operators
// !== strict inequality operators

const PI = 3.14;
if(PI !== "3.14"){
    console.log("that is not pi");
}
else{
    console.log("that is pi");
}